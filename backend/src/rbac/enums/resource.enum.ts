/**
 * Enum que define os recursos disponíveis no sistema baseados nas páginas da sidebar
 * Estes recursos são usados para controle de permissões
 */
export enum Resource {
  DASHBOARD = 'dashboard',
  USUARIOS = 'usuarios',
  RBAC = 'rbac',
  BLOG = 'blog',
  CUPONS = 'cupons',
  PEDIDOS = 'pedidos',
  INTEGRACOES = 'integracoes',
  PAGINAS = 'paginas',
}
